session_name: components
start_directory: "~/Repos/component-library/"
windows:
  - window_name: code
    panes:
      - shell_command:
          - cmd: v
  - window_name: build
    layout: even-horizontal
    panes:
      - shell_command:
          - cmd: pnpm dev
      - shell_command:
