session_name: components
start_directory: "~/Repos/component-library/"
windows:
  - window_name: code
    panes:
      - shell_command:
          - cmd: echo "Starting Session ..."
          - cmd: echo "Loading ..."
          - cmd: v
  - window_name: build
    panes:
      - shell_command:
          - cmd: echo "Starting Session ..."
          - cmd: echo "Loading ..."
          - cmd: pnpm dev
  - window_name: shell
    panes:
      - shell_command:
          - cmd: echo "Starting Session ..."
          - cmd: clear
